import React from "react";
import { MedicalInfoInputs } from "../../health-form/metrics/MedicalInfoInputs";

export const MedicalHistoryStep = ({ form }) => {
  return <MedicalInfoInputs form={form} />;
};