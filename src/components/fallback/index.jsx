import { Loader2 } from "lucide-react";

const Loader = () => {
  return (
    <main className="container-center">
      <Loader2 className="animate-spin loader-black" />
    </main>
  );
};

export default Loader;
